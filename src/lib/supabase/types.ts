// Placeholder — regenerated from the live schema via
// `mcp__Supabase__generate_typescript_types` once migrations are applied.
// Keeping this as `any`-shaped until then so the client helpers type-check.
export type Database = Record<string, unknown>;
